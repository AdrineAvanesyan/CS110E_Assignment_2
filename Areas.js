  let height = 8;

 let width = 10;

 let base = 12;

  const trianglearea = (height * base ) / 2
  const rectanglearea = (width * height)

  if (trianglearea > rectanglearea) {
      console.log("The triangle has bigger area than rectangle")
  }
  else {
      console.log("The rectangle has bigger area than triangle")
  }
  if ( trianglearea = rectanlglearea) {
      console.log("Shapes are equal")
  }
